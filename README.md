# TaskManager-cicd

#To run in Ubuntu-

sudo apt install nodejs
sudo apt install npm
npm install
node app.js
app will run in localhost:8000

#To run in Windows-

Install node
npm install
node app.js
app will run in localhost:8000
